export class GameModel {
    constructor() {
        this.board = this.initializeBoard();
        // Autres états du jeu comme les positions des joueurs, murs, etc.
    }

    initializeBoard() {
        // Logique pour créer un plateau de jeu initial
    }

}
